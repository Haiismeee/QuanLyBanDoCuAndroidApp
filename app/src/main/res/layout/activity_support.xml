<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FAFAFA"
    android:padding="24dp">

    <!-- TITLE -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Hỗ trợ khách hàng"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="#ff4b3a"
        android:layout_marginBottom="16dp" />

    <!-- DESCRIPTION -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Nếu bạn gặp sự cố trong quá trình sử dụng ứng dụng, vui lòng liên hệ với chúng tôi qua các kênh bên dưới."
        android:textSize="14sp"
        android:textColor="#444444"
        android:layout_marginBottom="24dp" />

    <!-- HOTLINE -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="📞 Hotline: 0901 234 567"
        android:textSize="15sp"
        android:textColor="#000000"
        android:layout_marginBottom="8dp" />

    <!-- EMAIL -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="✉️ Email: support@qlybandocu.com"
        android:textSize="15sp"
        android:textColor="#000000"
        android:layout_marginBottom="32dp" />

    <!-- CALL BUTTON -->
    <Button
        android:id="@+id/btnCall"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="📞 Gọi hotline"
        android:textAllCaps="false"
        android:backgroundTint="#FF4B3A"
        android:textColor="@android:color/white"
        android:layout_marginBottom="16dp"/>

    <!-- EMAIL BUTTON -->
    <Button
        android:id="@+id/btnEmail"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="✉️ Gửi email hỗ trợ"
        android:textAllCaps="false"
        android:backgroundTint="#FF4B3A"
        android:textColor="@android:color/white"/>

</LinearLayout>
